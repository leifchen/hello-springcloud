spring:
  cloud:
    config:
      name: config
      profile: dev
      #uri: http://localhost:8001/
      discovery:
        enabled: true
        service-id: spring-cloud-config-server

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/